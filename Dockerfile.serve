FROM python:3.11-slim AS serve

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY pyproject.toml README.md ./
COPY src ./src

RUN uv sync --all-extras

EXPOSE 8000

CMD ["uv", "run", "uvicorn", "energy_price_mlops.serving.app:app", "--host", "0.0.0.0", "--port", "8000"]

